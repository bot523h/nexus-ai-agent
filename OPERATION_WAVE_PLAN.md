<!-- GENERATED by `python -m nexus_ai_agent.nagar --docs` from the live sources via OPERATION_TRUTH.json. DO NOT EDIT: edits are detected by tests/architecture/test_operation_truth_gate.py and turn CI red. -->

# Nagar Operation Wave Plan (computed missing set)

The membership of every wave below is **recomputed** from
`missing_set` in `OPERATION_TRUTH.json` (catalogue − registry), with the
wave chosen by measured facts only: whether the catalogue's engine cell
names a model, whether the pack already ships, the type-level dependency
edges, and the catalogue permission level. Engineering order here is
*not* product priority — prerequisites are explicit so neither can be
smuggled in as the other.

The universal per-wave prerequisite chain is:

```
contract (OperationSpec + input model)
  -> runtime (handler + registrar wiring)
  -> verifier (pack test + boundary test + truth recompute)
  -> surface (entrypoint mapping, when the wave calls for one)
  -> E2E (recorded proof entry in OPERATION_PROOF_REGISTRY.json)
```

`IMPLEMENTATION-BLOCKED` marks an operation whose contract cannot honestly
start before its infrastructure prerequisite exists (a model lane, a pack
manifest). Such an operation stays in the wave it is *blocked for* with the
blocker named — it is never quietly moved earlier by a documentation edit.

## NOW (3 operations)

**NOW** — no model required, the pack already ships, and no missing operation blocks it: only the operation itself (spec → handler → verifier) is missing. Lowest risk: existing pack substrate, existing lane, existing test conventions.

| operation | pack | dependency | lane | test requirement | locality | security | complexity | prerequisites |
|---|---|---|---|---|---|---|---|---|
| `color.deband_denoise` | nexus.color.delivery | operation_registration | pure reducer / typed domain handler | `tests/unit/test_color_pack.py + tests/architecture/test_color_pack_boundary.py` | LOCAL_ONLY | permission level B per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; |
| `color.hdr_tonemap` | nexus.color.delivery | operation_registration | master (FFmpeg/native) | `tests/unit/test_color_pack.py + tests/architecture/test_color_pack_boundary.py` | LOCAL_ONLY | permission level B per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; |
| `color.white_balance` | nexus.color.delivery | operation_registration | pure reducer / typed domain handler | `tests/unit/test_color_pack.py + tests/architecture/test_color_pack_boundary.py` | LOCAL_ONLY | permission level B per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; |

## NEXT (4 operations)

**NEXT** — no model required, but the pack itself does not ship yet: manifest + registrar composition come before any operation work (pack-before-operation is the composition rule the runtime enforces).

| operation | pack | dependency | lane | test requirement | locality | security | complexity | prerequisites |
|---|---|---|---|---|---|---|---|---|
| `portrait.background_blur` | nexus.vision.portrait | pack_registration | preview (WebGPU/WebCodecs) + master fallback | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_ONLY | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: pack nexus.vision.portrait must ship first |
| `scene.detect_shot_boundaries` | nexus.vision.scene | pack_registration | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_ONLY | permission level A per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: pack nexus.vision.scene must ship first |
| `scene.find_subject_moment` | nexus.vision.scene | pack_registration | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_ONLY | permission level A per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: pack nexus.vision.scene must ship first |
| `scene.track_object` | nexus.vision.scene | pack_registration | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_ONLY | permission level A per catalogue | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: pack nexus.vision.scene must ship first |

## AFTER_VERTICAL_SLICE (5 operations)

**AFTER VERTICAL SLICE** — blocked on a family vertical slice: the model-dependent primitives other missing operations consume (output-type edges below), and the operations whose inputs come from those primitives even when they need no model themselves. Each primitive needs the model lane (fetch → hash → license → cache → offline run) first; the slice is proven end-to-end contract → runtime → verifier → E2E with a recorded proof before its dependents start.

| operation | pack | dependency | lane | test requirement | locality | security | complexity | prerequisites |
|---|---|---|---|---|---|---|---|---|
| `portrait.detect_landmarks` | nexus.vision.portrait | model_runtime | analysis (ONNX/local model runtime) | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level A per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `portrait.enhance_eyes` | nexus.vision.portrait | pack_registration | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_ONLY | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | reducer or filtergraph-level (no model) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: pack nexus.vision.portrait must ship first |
| `portrait.mask_hair` | nexus.vision.portrait | model_runtime | analysis (ONNX/local model runtime) | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level A per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.segment_subject` | nexus.vision.scene | model_runtime | analysis (ONNX/local model runtime) | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level A per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.track_face` | nexus.vision.scene | model_runtime | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level A per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |

## POST_MVP (9 operations)

**POST-MVP** — model-dependent dependents/orphans below confirmation level: implement after the family primitive's slice is proven.

| operation | pack | dependency | lane | test requirement | locality | security | complexity | prerequisites |
|---|---|---|---|---|---|---|---|---|
| `portrait.relight_face` | nexus.vision.portrait | model_runtime | analysis (ONNX/local model runtime) | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `portrait.retouch_blemish` | nexus.vision.portrait | model_runtime | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `portrait.smooth_skin` | nexus.vision.portrait | model_runtime | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `portrait.stabilize_face` | nexus.vision.portrait | model_runtime | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `portrait.whiten_teeth` | nexus.vision.portrait | model_runtime | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.auto_reframe_subject` | nexus.vision.scene | model_runtime | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.remove_background` | nexus.vision.scene | model_runtime | analysis (ONNX/local model runtime) | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.remove_object` | nexus.vision.scene | model_runtime | master (FFmpeg/native) | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.replace_sky` | nexus.vision.scene | model_runtime | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level B per catalogue | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |

## LATER (2 operations)

**LATER** — model-dependent and catalogue permission level C (confirmation-heavy identity semantics): the confirmation contract and the model lane must both be proven before design starts.

| operation | pack | dependency | lane | test requirement | locality | security | complexity | prerequisites |
|---|---|---|---|---|---|---|---|---|
| `portrait.correct_gaze` | nexus.vision.portrait | model_runtime | pure reducer / typed domain handler | `tests/unit/test_portrait_pack.py + tests/architecture/test_portrait_pack_boundary.py` | LOCAL_PREFERRED | permission level C per catalogue; identity-sensitive (face data must stay local, confirm below confidence threshold) | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |
| `scene.remove_logo` | nexus.vision.scene | model_runtime | pure reducer / typed domain handler | `tests/unit/test_scene_pack.py + tests/architecture/test_scene_pack_boundary.py` | LOCAL_PREFERRED | permission level C per catalogue; requires explicit confirmation before dispatch | model-dependent (needs a local model lane: fetch/hash/license/cache) | contract -> runtime -> verifier -> surface -> E2E; IMPLEMENTATION-BLOCKED: model lane (fetch/hash/license/cache) |

## Type-level dependency edges (catalogue Input → Output, mechanical)

An edge `a -> b` exists when `b`'s input cell names a type `a`'s output
cell produces. Renaming a type rewrites this graph; nothing is curated.

| from | to | via types |
|---|---|---|
| `audio.detect_beats` | `audio.align_music` | BeatGrid |
| `audio.detect_beats` | `audio.beat_sync_cut` | BeatGrid |
| `audio.detect_beats` | `timeline.retime_to_music` | BeatGrid |
| `caption.align_words` | `caption.generate_srt` | WordTimingSet |
| `caption.align_words` | `caption.highlight_words` | WordTimingSet |
| `caption.style_vazirmatn` | `caption.burn_in` | StyledCaptionAsset |
| `caption.transcribe` | `caption.align_words` | TranscriptRef |
| `caption.transcribe` | `caption.generate_ass_rtl` | TranscriptRef |
| `caption.transcribe` | `caption.search_transcript` | TranscriptRef |
| `caption.transcribe` | `caption.translate_local` | TranscriptRef |
| `motion.stabilize` | `motion.keyframe_transform` | TransformCurve |
| `portrait.detect_landmarks` | `portrait.correct_gaze` | FaceTrackSet |
| `portrait.detect_landmarks` | `portrait.enhance_eyes` | FaceTrackSet |
| `portrait.detect_landmarks` | `portrait.mask_hair` | FaceTrackSet |
| `portrait.detect_landmarks` | `portrait.relight_face` | FaceTrackSet |
| `portrait.detect_landmarks` | `portrait.smooth_skin` | FaceTrackSet |
| `portrait.detect_landmarks` | `portrait.stabilize_face` | FaceTrackSet |
| `portrait.mask_hair` | `motion.apply_mask` | MaskRef |
| `portrait.mask_hair` | `portrait.smooth_skin` | MaskRef |
| `portrait.stabilize_face` | `motion.keyframe_transform` | TransformCurve |
| `scene.auto_reframe_subject` | `motion.keyframe_transform` | TransformCurve |
| `scene.segment_subject` | `motion.apply_mask` | MaskRef |
| `scene.segment_subject` | `portrait.smooth_skin` | MaskRef |
| `scene.track_face` | `portrait.correct_gaze` | FaceTrackSet |
| `scene.track_face` | `portrait.enhance_eyes` | FaceTrackSet |
| `scene.track_face` | `portrait.mask_hair` | FaceTrackSet |
| `scene.track_face` | `portrait.relight_face` | FaceTrackSet |
| `scene.track_face` | `portrait.smooth_skin` | FaceTrackSet |
| `scene.track_face` | `portrait.stabilize_face` | FaceTrackSet |
| `scene.track_object` | `scene.remove_object` | ObjectTrack |
| `timeline.retime_to_music` | `timeline.speed_ramp` | RateCurve |
