<!-- GENERATED by `python -m nexus_ai_agent.nagar --docs` from the live sources via OPERATION_TRUTH.json. DO NOT EDIT: edits are detected by tests/architecture/test_operation_truth_gate.py and turn CI red. -->

# Nagar L0–Maturity Matrix (canonical ladder)

This page is rendered from the **single** ladder definition owned by
`src/nexus_ai_agent/nagar/truth.py::LADDER`. The open pull requests defined
`L3`/`L4` incompatibly; this is the resolution — no bare level token from any
other document is portable here.

## The ladder

* **L0** — design: a contract exists — the row appears in the product catalogue (docs/NAGAR_70_OPERATIONS_TDD.md pack tables) or a registered OperationSpec
* **L1** — code: registered in build_runtime_registry() and domain-ready (typed input model with extra="forbid", a named non-lambda handler, deterministic=True)
* **L2** — reachable: a live user-facing entrypoint (Telegram command → worker map, or /slideshow service) names the operation
* **L3** — operational: L2 and executable proof — the test suite executes the operation (dispatch/handler/service call sites) or the recorded proof registry carries an execution proof for it
* **L4** — production-like: L3 and a recorded artifact proof and a production-like measurement; NO repository source defines or measures production-like today, so this engine never emits L4

**Why no L4 appears below:** no repository source defines or measures a production-like level; recording L4 would be an unsourced claim

## Distribution (recomputed, never stored)

| level | count |
|---|---|
| L0 | 23 |
| L1 | 45 |
| L2 | 0 |
| L3 | 12 |
| L4 | 0 |

## Per-operation levels

| level | operations |
|---|---|
| L0 (23) | `color.deband_denoise`, `color.hdr_tonemap`, `color.white_balance`, `portrait.background_blur`, `portrait.correct_gaze`, `portrait.detect_landmarks`, `portrait.enhance_eyes`, `portrait.mask_hair`, `portrait.relight_face`, `portrait.retouch_blemish`, `portrait.smooth_skin`, `portrait.stabilize_face`, `portrait.whiten_teeth`, `scene.auto_reframe_subject`, `scene.detect_shot_boundaries`, `scene.find_subject_moment`, `scene.remove_background`, `scene.remove_logo`, `scene.remove_object`, `scene.replace_sky`, `scene.segment_subject`, `scene.track_face`, `scene.track_object` |
| L1 (45) | `audio.align_music`, `audio.beat_sync_cut`, `audio.deess`, `audio.detect_beats`, `audio.duck_music`, `audio.eq_voice`, `audio.normalize_loudness`, `audio.remove_noise`, `audio.remove_vocal`, `audio.time_stretch`, `caption.align_words`, `caption.burn_in`, `caption.diarize`, `caption.generate_ass_rtl`, `caption.generate_srt`, `caption.highlight_words`, `caption.search_transcript`, `caption.style_vazirmatn`, `caption.translate_local`, `color.apply_lut`, `color.auto_balance`, `color.match_shot`, `delivery.render_master_4k`, `media.pause`, `media.play`, `motion.add_glow`, `motion.add_motion_blur`, `motion.add_parallax`, `motion.add_particles`, `motion.add_title`, `motion.add_transition`, `motion.apply_mask`, `motion.keyframe_transform`, `motion.stabilize`, `motion.warp`, `slideshow.upscale`, `system.undo`, `timeline.attach_b_roll`, `timeline.freeze_frame`, `timeline.insert_gap`, `timeline.mark`, `timeline.retime_to_music`, `timeline.ripple_delete`, `timeline.split_at_playhead`, `timeline.sync_multicam` |
| L2 (0) | — |
| L3 (12) | `caption.transcribe`, `color.adjust_exposure`, `delivery.export_otio`, `delivery.make_proxy_480p`, `slideshow.compose`, `slideshow.render`, `slideshow.scan_assets`, `slideshow.score_images`, `slideshow.suggest_tone`, `timeline.reverse_segment`, `timeline.speed_ramp`, `timeline.trim` |
| L4 (0) | — |
